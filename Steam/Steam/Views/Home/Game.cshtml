@model GameModel
@using Steam.Models;
@{
    ViewBag.Title = "Game";
}

<h2>Game</h2>

<div>
    Title: @Model.title
    <br>
    price: @Model.prijs
    <br>
    release date: @Model.releaseDate
    <br>
    <a href="@Url.Action("AddToCart", "Home", new {id = Model.id})">Voeg toe aan winkelwagen</a>
    <br>
    <a href="@Url.Action("Review", "Home", new {id = Model.id})">Review deze game</a>

    @*@foreach(ReviewModel review in Model.reviews)
    {
        @review.inhoud
        @review.reviewedBy
        @review.aanbevolen
        @review.datum
        <br>
    }*@
</div>